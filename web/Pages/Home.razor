@page "/"
@attribute [Authorize]

<PageTitle>Inicio | BSC</PageTitle>

<AuthorizeView>
    <h1>Bienvenido, @context.User.Identity?.Name</h1>
    <p class="text-muted">Perfil: @context.User.FindFirst("role")?.Value</p>
</AuthorizeView>